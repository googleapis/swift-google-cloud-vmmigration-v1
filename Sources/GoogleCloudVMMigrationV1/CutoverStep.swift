// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// CutoverStep holds information about the cutover step progress.
public struct CutoverStep: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// The time the step has started.
  public var startTime: GoogleCloudWKT.Timestamp? = nil

  /// The time the step has ended.
  public var endTime: GoogleCloudWKT.Timestamp? = nil

  public var step: OneOf_Step? = nil

  /// Initialize a new instance of `CutoverStep`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = CutoverStep().with { $0.previousReplicationCycle = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case previousReplicationCycle = "previousReplicationCycle"
    case shuttingDownSourceVm = "shuttingDownSourceVm"
    case finalSync = "finalSync"
    case preparingVmDisks = "preparingVmDisks"
    case instantiatingMigratedVm = "instantiatingMigratedVm"
    case startTime = "startTime"
    case endTime = "endTime"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.startTime = try container.decodeIfPresent(
      GoogleCloudWKT.Timestamp.self, forKey: .startTime)
    self.endTime = try container.decodeIfPresent(GoogleCloudWKT.Timestamp.self, forKey: .endTime)

    var step: OneOf_Step? = nil
    let stepCheckAndSet = {
      if step != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath, debugDescription: "Multiple values set for oneof 'step'"
          ))
      }
      step = $0
    }
    if let previousReplicationCycle = try container.decodeIfPresent(
      ReplicationCycle?.self, forKey: .previousReplicationCycle)
    {
      try stepCheckAndSet(.previousReplicationCycle(previousReplicationCycle))
    }
    if let shuttingDownSourceVm = try container.decodeIfPresent(
      ShuttingDownSourceVMStep?.self, forKey: .shuttingDownSourceVm)
    {
      try stepCheckAndSet(.shuttingDownSourceVm(shuttingDownSourceVm))
    }
    if let finalSync = try container.decodeIfPresent(ReplicationCycle?.self, forKey: .finalSync) {
      try stepCheckAndSet(.finalSync(finalSync))
    }
    if let preparingVmDisks = try container.decodeIfPresent(
      PreparingVMDisksStep?.self, forKey: .preparingVmDisks)
    {
      try stepCheckAndSet(.preparingVmDisks(preparingVmDisks))
    }
    if let instantiatingMigratedVm = try container.decodeIfPresent(
      InstantiatingMigratedVMStep?.self, forKey: .instantiatingMigratedVm)
    {
      try stepCheckAndSet(.instantiatingMigratedVm(instantiatingMigratedVm))
    }
    self.step = step
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.startTime, forKey: .startTime)
    try container.encode(self.endTime, forKey: .endTime)

    if let choice = self.step {
      switch choice {
      case .previousReplicationCycle(let value):
        try container.encode(value, forKey: .previousReplicationCycle)
      case .shuttingDownSourceVm(let value):
        try container.encode(value, forKey: .shuttingDownSourceVm)
      case .finalSync(let value):
        try container.encode(value, forKey: .finalSync)
      case .preparingVmDisks(let value):
        try container.encode(value, forKey: .preparingVmDisks)
      case .instantiatingMigratedVm(let value):
        try container.encode(value, forKey: .instantiatingMigratedVm)
      }
    }
  }

  public enum OneOf_Step: Codable, Equatable, Sendable {
    /// A replication cycle prior cutover step.
    indirect case previousReplicationCycle(ReplicationCycle?)
    /// Shutting down VM step.
    indirect case shuttingDownSourceVm(ShuttingDownSourceVMStep?)
    /// Final sync step.
    indirect case finalSync(ReplicationCycle?)
    /// Preparing VM disks step.
    indirect case preparingVmDisks(PreparingVMDisksStep?)
    /// Instantiating migrated VM step.
    indirect case instantiatingMigratedVm(InstantiatingMigratedVMStep?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.vmmigration.v1.CutoverStep"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
