// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWkt
import GoogleRpc

/// MigratingVm describes the VM that will be migrated from a Source environment
/// and its replication state.
public struct MigratingVm: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Output only. The identifier of the MigratingVm.
  public var name: Swift.String = Swift.String()

  /// The unique ID of the VM in the source.
  /// The VM's name in vSphere can be changed, so this is not the VM's name but
  /// rather its moRef id. This id is of the form vm-<num>.
  public var sourceVmId: Swift.String = Swift.String()

  /// The display name attached to the MigratingVm by the user.
  public var displayName: Swift.String = Swift.String()

  /// The description attached to the migrating VM by the user.
  public var description: Swift.String = Swift.String()

  /// The replication schedule policy.
  public var policy: SchedulePolicy? = nil

  /// Output only. The time the migrating VM was created (this refers to this
  /// resource and not to the time it was installed in the source).
  public var createTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. The last time the migrating VM resource was updated.
  public var updateTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. The most updated snapshot created time in the source that
  /// finished replication.
  public var lastSync: ReplicationSync? = nil

  /// Output only. State of the MigratingVm.
  public var state: MigratingVm.State = MigratingVm.State()

  /// Output only. The last time the migrating VM state was updated.
  public var stateTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. Details of the current running replication cycle.
  public var currentSyncInfo: ReplicationCycle? = nil

  /// Output only. Details of the last replication cycle. This will be updated
  /// whenever a replication cycle is finished and is not to be confused with
  /// last_sync which is only updated on successful replication cycles.
  public var lastReplicationCycle: ReplicationCycle? = nil

  /// Output only. The group this migrating vm is included in, if any. The group
  /// is represented by the full path of the appropriate
  /// [Group][google.cloud.vmmigration.v1.Group] resource.
  ///
  /// [google.cloud.vmmigration.v1.Group]: <doc:Group>
  public var group: Swift.String = Swift.String()

  /// The labels of the migrating VM.
  public var labels: [Swift.String: Swift.String] = [:]

  /// Output only. The recent [clone jobs][google.cloud.vmmigration.v1.CloneJob]
  /// performed on the migrating VM. This field holds the vm's last completed
  /// clone job and the vm's running clone job, if one exists.
  /// Note: To have this field populated you need to explicitly request it via
  /// the "view" parameter of the Get/List request.
  ///
  /// [google.cloud.vmmigration.v1.CloneJob]: <doc:CloneJob>
  public var recentCloneJobs: [CloneJob] = []

  /// Output only. Provides details on the state of the Migrating VM in case of
  /// an error in replication.
  public var error: GoogleRpc.Status? = nil

  /// Output only. The recent cutover jobs performed on the migrating VM.
  /// This field holds the vm's last completed cutover job and the vm's
  /// running cutover job, if one exists.
  /// Note: To have this field populated you need to explicitly request it via
  /// the "view" parameter of the Get/List request.
  public var recentCutoverJobs: [CutoverJob] = []

  /// Output only. Provides details of future CutoverJobs of a MigratingVm.
  /// Set to empty when cutover forecast is unavailable.
  public var cutoverForecast: CutoverForecast? = nil

  /// Output only. Provides details about the expiration state of the migrating
  /// VM.
  public var expiration: MigratingVm.Expiration? = nil

  /// The default configuration of the target VM that will be created in Google
  /// Cloud as a result of the migration.
  public var targetVmDefaults: OneOf_TargetVmDefaults? = nil

  /// Details about the source VM.
  public var sourceVmDetails: OneOf_SourceVmDetails? = nil

  /// Initialize a new instance of `MigratingVm`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = MigratingVm().with { $0.computeEngineTargetDefaults = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case computeEngineTargetDefaults = "computeEngineTargetDefaults"
    case computeEngineDisksTargetDefaults = "computeEngineDisksTargetDefaults"
    case vmwareSourceVmDetails = "vmwareSourceVmDetails"
    case awsSourceVmDetails = "awsSourceVmDetails"
    case azureSourceVmDetails = "azureSourceVmDetails"
    case name = "name"
    case sourceVmId = "sourceVmId"
    case displayName = "displayName"
    case description = "description"
    case policy = "policy"
    case createTime = "createTime"
    case updateTime = "updateTime"
    case lastSync = "lastSync"
    case state = "state"
    case stateTime = "stateTime"
    case currentSyncInfo = "currentSyncInfo"
    case lastReplicationCycle = "lastReplicationCycle"
    case group = "group"
    case labels = "labels"
    case recentCloneJobs = "recentCloneJobs"
    case error = "error"
    case recentCutoverJobs = "recentCutoverJobs"
    case cutoverForecast = "cutoverForecast"
    case expiration = "expiration"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.name = try container.decode(Swift.String.self, forKey: .name)
    self.sourceVmId = try container.decode(Swift.String.self, forKey: .sourceVmId)
    self.displayName = try container.decode(Swift.String.self, forKey: .displayName)
    self.description = try container.decode(Swift.String.self, forKey: .description)
    self.policy = try container.decodeIfPresent(SchedulePolicy.self, forKey: .policy)
    self.createTime = try container.decodeIfPresent(
      GoogleCloudWkt.Timestamp.self, forKey: .createTime)
    self.updateTime = try container.decodeIfPresent(
      GoogleCloudWkt.Timestamp.self, forKey: .updateTime)
    self.lastSync = try container.decodeIfPresent(ReplicationSync.self, forKey: .lastSync)
    self.state = try container.decode(MigratingVm.State.self, forKey: .state)
    self.stateTime = try container.decodeIfPresent(
      GoogleCloudWkt.Timestamp.self, forKey: .stateTime)
    self.currentSyncInfo = try container.decodeIfPresent(
      ReplicationCycle.self, forKey: .currentSyncInfo)
    self.lastReplicationCycle = try container.decodeIfPresent(
      ReplicationCycle.self, forKey: .lastReplicationCycle)
    self.group = try container.decode(Swift.String.self, forKey: .group)
    self.labels = try container.decode([Swift.String: Swift.String].self, forKey: .labels)
    self.recentCloneJobs = try container.decode([CloneJob].self, forKey: .recentCloneJobs)
    self.error = try container.decodeIfPresent(GoogleRpc.Status.self, forKey: .error)
    self.recentCutoverJobs = try container.decode([CutoverJob].self, forKey: .recentCutoverJobs)
    self.cutoverForecast = try container.decodeIfPresent(
      CutoverForecast.self, forKey: .cutoverForecast)
    self.expiration = try container.decodeIfPresent(
      MigratingVm.Expiration.self, forKey: .expiration)

    var targetVmDefaults: OneOf_TargetVmDefaults? = nil
    let targetVmDefaultsCheckAndSet = {
      if targetVmDefaults != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'targetVmDefaults'"))
      }
      targetVmDefaults = $0
    }
    if let computeEngineTargetDefaults = try container.decodeIfPresent(
      ComputeEngineTargetDefaults?.self, forKey: .computeEngineTargetDefaults)
    {
      try targetVmDefaultsCheckAndSet(.computeEngineTargetDefaults(computeEngineTargetDefaults))
    }
    if let computeEngineDisksTargetDefaults = try container.decodeIfPresent(
      ComputeEngineDisksTargetDefaults?.self, forKey: .computeEngineDisksTargetDefaults)
    {
      try targetVmDefaultsCheckAndSet(
        .computeEngineDisksTargetDefaults(computeEngineDisksTargetDefaults))
    }
    self.targetVmDefaults = targetVmDefaults

    var sourceVmDetails: OneOf_SourceVmDetails? = nil
    let sourceVmDetailsCheckAndSet = {
      if sourceVmDetails != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'sourceVmDetails'"))
      }
      sourceVmDetails = $0
    }
    if let vmwareSourceVmDetails = try container.decodeIfPresent(
      VmwareSourceVmDetails?.self, forKey: .vmwareSourceVmDetails)
    {
      try sourceVmDetailsCheckAndSet(.vmwareSourceVmDetails(vmwareSourceVmDetails))
    }
    if let awsSourceVmDetails = try container.decodeIfPresent(
      AwsSourceVmDetails?.self, forKey: .awsSourceVmDetails)
    {
      try sourceVmDetailsCheckAndSet(.awsSourceVmDetails(awsSourceVmDetails))
    }
    if let azureSourceVmDetails = try container.decodeIfPresent(
      AzureSourceVmDetails?.self, forKey: .azureSourceVmDetails)
    {
      try sourceVmDetailsCheckAndSet(.azureSourceVmDetails(azureSourceVmDetails))
    }
    self.sourceVmDetails = sourceVmDetails
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.sourceVmId, forKey: .sourceVmId)
    try container.encode(self.displayName, forKey: .displayName)
    try container.encode(self.description, forKey: .description)
    try container.encode(self.policy, forKey: .policy)
    try container.encode(self.createTime, forKey: .createTime)
    try container.encode(self.updateTime, forKey: .updateTime)
    try container.encode(self.lastSync, forKey: .lastSync)
    try container.encode(self.state, forKey: .state)
    try container.encode(self.stateTime, forKey: .stateTime)
    try container.encode(self.currentSyncInfo, forKey: .currentSyncInfo)
    try container.encode(self.lastReplicationCycle, forKey: .lastReplicationCycle)
    try container.encode(self.group, forKey: .group)
    try container.encode(self.labels, forKey: .labels)
    try container.encode(self.recentCloneJobs, forKey: .recentCloneJobs)
    try container.encode(self.error, forKey: .error)
    try container.encode(self.recentCutoverJobs, forKey: .recentCutoverJobs)
    try container.encode(self.cutoverForecast, forKey: .cutoverForecast)
    try container.encode(self.expiration, forKey: .expiration)

    if let choice = self.targetVmDefaults {
      switch choice {
      case .computeEngineTargetDefaults(let value):
        try container.encode(value, forKey: .computeEngineTargetDefaults)
      case .computeEngineDisksTargetDefaults(let value):
        try container.encode(value, forKey: .computeEngineDisksTargetDefaults)
      }
    }

    if let choice = self.sourceVmDetails {
      switch choice {
      case .vmwareSourceVmDetails(let value):
        try container.encode(value, forKey: .vmwareSourceVmDetails)
      case .awsSourceVmDetails(let value):
        try container.encode(value, forKey: .awsSourceVmDetails)
      case .azureSourceVmDetails(let value):
        try container.encode(value, forKey: .azureSourceVmDetails)
      }
    }
  }

  /// Expiration holds information about the expiration of a MigratingVm.
  public struct Expiration: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Output only. Timestamp of when this resource is considered expired.
    public var expireTime: GoogleCloudWkt.Timestamp? = nil

    /// Output only. The number of times expiration was extended.
    public var extensionCount: Swift.Int32 = Swift.Int32()

    /// Output only. Describes whether the expiration can be extended.
    public var extendable: Swift.Bool = Swift.Bool()

    /// Initialize a new instance of `Expiration`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Expiration().with { $0.expireTime = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.vmmigration.v1.MigratingVm.Expiration"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// The possible values of the state/health of source VM.
  public enum State: Codable, Equatable, Sendable {
    /// The state was not sampled by the health checks yet.
    case unspecified
    /// The VM in the source is being verified.
    case pending
    /// The source VM was verified, and it's ready to start replication.
    case ready
    /// Migration is going through the first sync cycle.
    case firstSync
    /// The replication is active, and it's running or scheduled to run.
    case active
    /// The source VM is being turned off, and a final replication is currently
    /// running.
    case cuttingOver
    /// The source VM was stopped and replicated. The replication is currently
    /// paused.
    case cutover
    /// A cutover job is active and replication cycle is running the final sync.
    case finalSync
    /// The replication was paused by the user and no cycles are scheduled to
    /// run.
    case paused
    /// The migrating VM is being finalized and migration resources are being
    /// removed.
    case finalizing
    /// The replication process is done. The migrating VM is finalized and no
    /// longer consumes billable resources.
    case finalized
    /// The replication process encountered an unrecoverable error and was
    /// aborted.
    case error
    /// The migrating VM has passed its expiration date. It might be possible to
    /// bring it back to "Active" state by updating the TTL field. For more
    /// information, see the documentation.
    case expired
    /// The migrating VM's has been finalized and migration resources have been
    /// removed.
    case finalizedExpired
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .pending: return 1
      case .ready: return 2
      case .firstSync: return 3
      case .active: return 4
      case .cuttingOver: return 7
      case .cutover: return 8
      case .finalSync: return 9
      case .paused: return 10
      case .finalizing: return 11
      case .finalized: return 12
      case .error: return 13
      case .expired: return 14
      case .finalizedExpired: return 17
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "STATE_UNSPECIFIED"
      case .pending: return "PENDING"
      case .ready: return "READY"
      case .firstSync: return "FIRST_SYNC"
      case .active: return "ACTIVE"
      case .cuttingOver: return "CUTTING_OVER"
      case .cutover: return "CUTOVER"
      case .finalSync: return "FINAL_SYNC"
      case .paused: return "PAUSED"
      case .finalizing: return "FINALIZING"
      case .finalized: return "FINALIZED"
      case .error: return "ERROR"
      case .expired: return "EXPIRED"
      case .finalizedExpired: return "FINALIZED_EXPIRED"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:State/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "STATE_UNSPECIFIED": self = .unspecified
      case "PENDING": self = .pending
      case "READY": self = .ready
      case "FIRST_SYNC": self = .firstSync
      case "ACTIVE": self = .active
      case "CUTTING_OVER": self = .cuttingOver
      case "CUTOVER": self = .cutover
      case "FINAL_SYNC": self = .finalSync
      case "PAUSED": self = .paused
      case "FINALIZING": self = .finalizing
      case "FINALIZED": self = .finalized
      case "ERROR": self = .error
      case "EXPIRED": self = .expired
      case "FINALIZED_EXPIRED": self = .finalizedExpired
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:State/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .pending
      case 2: self = .ready
      case 3: self = .firstSync
      case 4: self = .active
      case 7: self = .cuttingOver
      case 8: self = .cutover
      case 9: self = .finalSync
      case 10: self = .paused
      case 11: self = .finalizing
      case 12: self = .finalized
      case 13: self = .error
      case 14: self = .expired
      case 17: self = .finalizedExpired
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .pending: return try container.encode(1)
      case .ready: return try container.encode(2)
      case .firstSync: return try container.encode(3)
      case .active: return try container.encode(4)
      case .cuttingOver: return try container.encode(7)
      case .cutover: return try container.encode(8)
      case .finalSync: return try container.encode(9)
      case .paused: return try container.encode(10)
      case .finalizing: return try container.encode(11)
      case .finalized: return try container.encode(12)
      case .error: return try container.encode(13)
      case .expired: return try container.encode(14)
      case .finalizedExpired: return try container.encode(17)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// The default configuration of the target VM that will be created in Google
  /// Cloud as a result of the migration.
  public enum OneOf_TargetVmDefaults: Codable, Equatable, Sendable {
    /// Details of the target VM in Compute Engine.
    indirect case computeEngineTargetDefaults(ComputeEngineTargetDefaults?)
    /// Details of the target Persistent Disks in Compute Engine.
    indirect case computeEngineDisksTargetDefaults(ComputeEngineDisksTargetDefaults?)
  }

  /// Details about the source VM.
  public enum OneOf_SourceVmDetails: Codable, Equatable, Sendable {
    /// Output only. Details of the VM from a Vmware source.
    indirect case vmwareSourceVmDetails(VmwareSourceVmDetails?)
    /// Output only. Details of the VM from an AWS source.
    indirect case awsSourceVmDetails(AwsSourceVmDetails?)
    /// Output only. Details of the VM from an Azure source.
    indirect case azureSourceVmDetails(AzureSourceVmDetails?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.vmmigration.v1.MigratingVm"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
