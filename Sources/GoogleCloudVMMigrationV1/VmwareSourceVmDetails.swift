// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleWKT

/// Represent the source Vmware VM details.
public struct VmwareSourceVmDetails: Codable, Equatable, GoogleWKT._AnyPackable,
  Sendable
{
  /// Output only. The firmware type of the source VM.
  public var firmware: VmwareSourceVmDetails.Firmware = VmwareSourceVmDetails.Firmware()

  /// Output only. The total size of the disks being migrated in bytes.
  public var committedStorageBytes: Swift.Int64 = Swift.Int64()

  /// Output only. The disks attached to the source VM.
  public var disks: [VmwareSourceVmDetails.VmwareDiskDetails] = []

  /// Output only. Information about VM capabilities needed for some Compute
  /// Engine features.
  public var vmCapabilitiesInfo: VmCapabilities? = nil

  /// Output only. The VM architecture.
  public var architecture: VmArchitecture = VmArchitecture()

  @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

  /// Initialize a new instance of `VmwareSourceVmDetails`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = VmwareSourceVmDetails().with { $0.firmware = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private struct CodingKeys: CodingKey {
    var stringValue: Swift.String
    var intValue: Swift.Int? { nil }
    init(stringValue: Swift.String) { self.stringValue = stringValue }
    init?(intValue: Swift.Int) { nil }

    static let firmware = CodingKeys(stringValue: "firmware")
    static let committedStorageBytes = CodingKeys(stringValue: "committedStorageBytes")
    static let disks = CodingKeys(stringValue: "disks")
    static let vmCapabilitiesInfo = CodingKeys(stringValue: "vmCapabilitiesInfo")
    static let architecture = CodingKeys(stringValue: "architecture")

    static let _knownKeys: Set<Swift.String> = [
      "firmware",
      "committedStorageBytes",
      "disks",
      "vmCapabilitiesInfo",
      "architecture",
    ]
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    if let value = try container.decodeIfPresent(
      VmwareSourceVmDetails.Firmware.self, forKey: .firmware)
    {
      self.firmware = value
    }
    if let value = try container.decodeIfPresent(Swift.Int64.self, forKey: .committedStorageBytes) {
      self.committedStorageBytes = value
    }
    if let value = try container.decodeIfPresent(
      [VmwareSourceVmDetails.VmwareDiskDetails].self, forKey: .disks)
    {
      self.disks = value
    }
    self.vmCapabilitiesInfo = try container.decodeIfPresent(
      VmCapabilities.self, forKey: .vmCapabilitiesInfo)
    if let value = try container.decodeIfPresent(VmArchitecture.self, forKey: .architecture) {
      self.architecture = value
    }
    for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
      self._unknownFields.json[key.stringValue] = try container.decode(
        GoogleWKT.Value.self, forKey: key)
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.firmware, forKey: .firmware)
    try container.encode(self.committedStorageBytes, forKey: .committedStorageBytes)
    try container.encode(self.disks, forKey: .disks)
    try container.encodeIfPresent(self.vmCapabilitiesInfo, forKey: .vmCapabilitiesInfo)
    try container.encode(self.architecture, forKey: .architecture)
    for (key, value) in self._unknownFields.json {
      try container.encode(value, forKey: CodingKeys(stringValue: key))
    }
  }

  /// The details of a Vmware VM disk.
  public struct VmwareDiskDetails: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// Output only. The ordinal number of the disk.
    public var diskNumber: Swift.Int32 = Swift.Int32()

    /// Output only. Size in GB.
    public var sizeGb: Swift.Int64 = Swift.Int64()

    /// Output only. The disk label.
    public var label: Swift.String = Swift.String()

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `VmwareDiskDetails`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = VmwareDiskDetails().with { $0.diskNumber = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let diskNumber = CodingKeys(stringValue: "diskNumber")
      static let sizeGb = CodingKeys(stringValue: "sizeGb")
      static let label = CodingKeys(stringValue: "label")

      static let _knownKeys: Set<Swift.String> = [
        "diskNumber",
        "sizeGb",
        "label",
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      if let value = try container.decodeIfPresent(Swift.Int32.self, forKey: .diskNumber) {
        self.diskNumber = value
      }
      if let value = try container.decodeIfPresent(Swift.Int64.self, forKey: .sizeGb) {
        self.sizeGb = value
      }
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .label) {
        self.label = value
      }
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.diskNumber, forKey: .diskNumber)
      try container.encode(self.sizeGb, forKey: .sizeGb)
      try container.encode(self.label, forKey: .label)
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.vmmigration.v1.VmwareSourceVmDetails.VmwareDiskDetails"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }

  /// Possible values for Vmware VM firmware.
  public enum Firmware: Codable, Equatable, Sendable {
    /// The firmware is unknown.
    case unspecified
    /// The firmware is EFI.
    case efi
    /// The firmware is BIOS.
    case bios
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .efi: return 1
      case .bios: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "FIRMWARE_UNSPECIFIED"
      case .efi: return "EFI"
      case .bios: return "BIOS"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Firmware/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "FIRMWARE_UNSPECIFIED": self = .unspecified
      case "EFI": self = .efi
      case "BIOS": self = .bios
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Firmware/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .efi
      case 2: self = .bios
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode("FIRMWARE_UNSPECIFIED")
      case .efi: return try container.encode("EFI")
      case .bios: return try container.encode("BIOS")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.vmmigration.v1.VmwareSourceVmDetails"
  }
  public init(fromAny any: GoogleWKT.`Any`) throws {
    self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleWKT.Struct {
    return try GoogleWKT._slowAnySerialize(message: self)
  }
}
